##About get_agent_info()

1. Must return :
    i. agent_list : list of all agents present in scene (each AgentMetadata contains the start and end times though)
    ii. agent_presence : list of "timesteps". Each timestep is a list of all Agents present (active) at that timestep. yes, each AgentMetadata still has info on its start and end times.

10. curr_agent_data has shape of (T, 9) with T==number of time steps. Ab uske andar chaahe values None hogi, toh it'll be removed in end after converted to DataFrame
    all_agent_data = List of each curr_agent_data
    agent_id u matintain as a separate list cuz last mei it appends to all_agent_data_df as the index, not the columns

3. Also, Remove agents who are there for 0 timesteps lol 

9. interpolate_array() is used on raw data to fill in missing INSIDE values from the data

4. extent=VariableExtent() is passed when it tries to say that get your extent from some cached DataFrame instead (idk which exactly, YET), as its not Fixed (else use FixedExtent). handled dynamically at runtime most probably.

5. all_agent_data_df 
    -> is pd.DataFrame containing of all agent's info in a given scene.
    -> It is multi-indexed : 
        -> agent_ids = id of the agent (A1,A1,A1,....A2,A2,A2,.....)
        -> agent_frame_ids = time step (0,1,2,......, 0,1,2,.....)
    -> Columns of the dataframe are : traj_cols + extent_cols
    -> It is to be exported to cache, as frequent loading and derivative computation is expensive.
    -> agent_ids is a list of the valid/clean ids btw.

6. While creating all_agent_data_df initially, we must ensure that it has full complete structure of rows=(scenario.num_agents*scenario.len_timesteps)
    We then remove the missing-values rows AFTER this dataframe created. Trajata can then know the time of appearence of the agent simply by the first indice containing that index.

10. Acceleration have computed with trajdata's internal function called agent_aware_diff. Works on the dataframe. 
    ->Do that computation before caching only, yep. 

11. Caching done by cache_class.save_agent_data(). Format is according to final_cols
    -> Traffic light data may also be saved, maybe I'll skip for now though.
    -> Also, cache_class.save_agent_data() is used from df_cache ka implemenation, not scene_cache. As cache_class will be passed as DataFrameCache instead of SceneCache.

7. QUESTION : 
    -> what does "Prediction" in commonroad mean? like its the raw actual data or does it literally predict something?
    -> Need it for DynamicObstacle.occupancy_at_time() 

8. QUESTION :
    -> how do i get info about the velocity of a DynamicObstacle (Agent) in CommonRoad? 
    -> Also other info about it like presence (in which time steps its active), ID of the agent, heading.

9. QUESTION : 
    -> There were so many different types of "State" in commonroad, how does that work?
    -> Is it like the same (raw) information of an Agent(at t=t) expressable in differnt forms? Or is it that different "types" of Agents(==DynamicObstacle) expressed in different States only?
    -> Which subclass should i use for getting only the required columns?

10. QUESTION : Some confusion/inconsistencies when converting ObstacleType (CommonRoad) to AgentType (trajdata) --> check code for the same.

11. QUESTION : 
        -> Ego vehicle is not classified as a Scenario.DynamicObstacle right?? but its present as a separate vehicle i believe so prolly not a part of that right?
        -> How to get ID of ego vehicle anyways?

12. DOUBT : In trajdata, is it necessary for ego vehicle information (states etc) to be present in agent_list and agent_presence ? Basically, is it supposed to be taken as an agent?

13. Yet to decide if should cache traffic light data or not etc.

14. NOTE: Must include some check (at the start) to check if the Scneario's DynamicObstacle has a TrajectoryPrediction or a SetBasedPrediction, only include obstacles with the former one.